import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./compact-trash.svg";

const CompactIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default CompactIcon;
