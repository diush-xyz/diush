import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./wait-icon.svg";

const WaitIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default WaitIcon;
