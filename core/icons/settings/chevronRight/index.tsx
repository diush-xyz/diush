import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./settings-chevron-right.svg";

const SettingsChevronRight = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default SettingsChevronRight;
