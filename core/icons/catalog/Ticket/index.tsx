import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./ticket.svg";

const TicketIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default TicketIcon;
