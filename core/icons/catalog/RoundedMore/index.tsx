import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./rounded-more.svg";

const RoundedMoreIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default RoundedMoreIcon;
