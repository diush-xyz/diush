import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./carousel.svg";

const CarouselIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default CarouselIcon;
