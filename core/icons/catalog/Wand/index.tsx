import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./wand.svg";

const WandIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default WandIcon;
