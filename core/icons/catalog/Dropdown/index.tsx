import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./dropdown.svg";

const DropdownIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default DropdownIcon;
