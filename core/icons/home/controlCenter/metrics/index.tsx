import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./metrics.svg";

const MetricsIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default MetricsIcon;
