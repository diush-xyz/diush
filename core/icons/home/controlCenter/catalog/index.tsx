import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./catalog.svg";

const CatalogIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default CatalogIcon;
