import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./myprofile.svg";

const MyProfileIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default MyProfileIcon;
