import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./compact-money.svg";

const MoneyCompactIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default MoneyCompactIcon;
