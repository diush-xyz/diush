import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./deals.svg";

const DealsIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default DealsIcon;
