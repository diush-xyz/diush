import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./leftarrow.svg";

const LeftArrowIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default LeftArrowIcon;
