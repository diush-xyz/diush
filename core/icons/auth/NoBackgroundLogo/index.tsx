import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./nobck.svg";

const NoBackgroundLogo = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default NoBackgroundLogo;
