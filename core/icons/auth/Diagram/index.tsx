import React from "react";
import { SvgProps } from "react-native-svg";
import Icon from "./diagram.svg";

const DiagramIcon = (props: SvgProps) => {
    return <Icon {...props} />;
};

export default DiagramIcon;
